/// Application constants shared by the UI and export services.
///
/// Keep this value aligned with the `version` field in pubspec.yaml whenever a
/// release is published. Exported scenario files use it for diagnostics only;
/// the file schema remains backward-compatible.
const String kAppVersion = '1.3.15+32';
