# التقرير الفني للمراجعة

## النطاق

تمت مراجعة تطبيق Flutter لسطح مكتب Windows، مع التركيز على نموذج السيناريوهات، التخزين والاستيراد والتصدير، حلقة التنفيذ، قناة MethodChannel، وإدخال لوحة المفاتيح والفأرة وإدارة النوافذ الأصلية.

## أهم المشكلات التي عولجت

| المجال | المشكلة | المعالجة |
|---|---|---|
| بناء Dart | استخدام `Timer` و`Completer` دون استيراد صريح لـ `dart:async` | إضافة الاستيراد الصريح |
| تعريف الحزمة | غياب اسم الحزمة في `pubspec.yaml` | إضافة `name: keyboard_automation` |
| الاعتمادات | وجود حزم غير مستخدمة | إزالة `path_provider` و`path` و`process_run` من الاعتمادات المباشرة |
| JSON | استخدام casts مباشرة قد تسبب انهياراً عند ملف تالف أو قيم نصية | إضافة تطبيع للأنواع وحدود للتكرار والتأخير والإحداثيات |
| التوافق | التصدير القديم كان قائمة خطوات بلا إصدار مخطط | إضافة مستند JSON بإصدار مخطط مع دعم الصيغة القديمة عند الاستيراد |
| الحافظة | الواجهة تستدعي `get_clipboard` دون معالج أصلي | إضافة قراءة Unicode من حافظة Windows |
| الإدخال | فشل `SendInput` كان يُهمل في النص والفأرة | إرجاع حالة النجاح وتحويل الفشل إلى خطأ واضح |
| المعدلات | المعدلات غير المعروفة كانت تُهمل بصمت | رفضها وإرجاع `INVALID_MODIFIERS` |
| إدارة النوافذ | قراءة العناوين عبر ANSI وتعريف `WindowInfo` مكرر | استخدام Unicode وتوحيد الترويسة والتنفيذ |
| CMake | تضمين `window_manager.cpp` داخل `flutter_window.cpp` | إضافة الملف إلى `SOURCES` واستخدام الترويسة فقط |
| الإيقاف | بعض الانتظارات كانت غير قابلة للإلغاء فوراً | توحيد الانتظار عبر `Completer` وإكماله عند الإيقاف أو الإغلاق |
| التشغيل | `Run.bat` يعتمد على مسار جهاز محدد وينفذ `clean` دائماً | جعله يعتمد على `%~dp0` وإضافة تحقق وتحليل قبل التشغيل |

## قرارات التصميم

تم الإبقاء على التطبيق في ملف Dart الرئيسي لتقليل مخاطرة إعادة هيكلة واسعة على مشروع مرفق صغير، مع تحسين الحدود الوظيفية ذات الأثر الأعلى. أما طبقة Windows فأصبحت أكثر استقلالاً؛ إذ يعلن كل ملف عن واجهته ويُبنى ضمن CMake بصورة تقليدية.

يتم رفض المعاملات غير الصالحة بدلاً من تحويلها إلى قيم قد تسبب سلوكاً مفاجئاً. وبالمثل، لا يُعتبر النقر أو الإدخال ناجحاً إلا إذا قبل Windows الأحداث المطلوبة. هذا يجعل الفشل قابلاً للرصد ويمنع إكمال سيناريو ناقص النتائج.

## إصلاح أخطاء البناء الأخيرة

تم تحديث استدعاءات `file_picker` لتتوافق مع الإصدار 12؛ إذ أصبحت عمليات الحفظ والاختيار تستخدم `FilePicker.saveFile` و`FilePicker.pickFiles` مباشرة، وأصبح الاختيار يعيد `List<PlatformFile>` بدلاً من `FilePickerResult`. كما تم تمرير محتوى JSON إلى `saveFile` عبر `Uint8List` و`mimeType` بدلاً من كتابة المسار يدوياً.

تم أيضاً تصحيح معامل `ListView.builder` من `builder` إلى `itemBuilder` في نافذة الاختصارات الجاهزة. وهذان التعديلان يعالجان أخطاء المترجم المشار إليها في أسطر 1427 و1461 و3100.

في طبقة Windows، تم وضع `NOMINMAX` قبل جميع التضمينات في ملفات C++ المتأثرة، مما يمنع تعريفات Windows لـ `min` و`max` من كسر `std::min` و`std::numeric_limits`. كما تم جعل متغيرات `INPUT` غير ثابتة قبل تمريرها إلى `SendInput`، وفق التوقيع المطلوب في Windows API.

## التحقق المنفذ

تم إجراء فحص نصي للرموز الحرجة وتطابق أسماء القنوات بين Dart وC++، ومراجعة إعدادات الحزمة وقائمة مصادر CMake، وإضافة اختبارات وحدات لنموذج البيانات. لم يتم تنفيذ `flutter analyze` أو `flutter test` داخل بيئة المراجعة لأن Flutter SDK غير متاح فيها؛ لذلك يجب تنفيذ أوامر التحقق التالية على جهاز Windows أو بيئة تحتوي على Flutter:

```powershell
flutter pub get
flutter analyze
flutter test
flutter build windows --release
```

## مرجع API خارجي

اعتمد تصحيح `file_picker` على توثيق الإصدار 12 الرسمي، الذي يوضح أن `FilePicker.pickFiles` يعيد قائمة `PlatformFile` وأن `FilePicker.saveFile` يستقبل `bytes` ويعيد `Uri` بدلاً من واجهة `FilePickerResult` القديمة. [1]

[1]: https://pub.dev/packages/file_picker

## معايير قبول عملية

تُعد النسخة جاهزة للاختبار الوظيفي عندما ينجح التحليل والاختبارات والبناء، ثم تُجرّب الحالات التالية: كتابة نص عربي وUnicode، قراءة الحافظة، ضغط `CTRL+C`، نقرة مزدوجة يميناً ويساراً، استيراد JSON قديم، استيراد JSON جديد، قيمة تكرار سالبة أو ضخمة، نافذة غير موجودة، وإيقاف السيناريو أثناء انتظار طويل.


## إعادة التصميم العربية وRTL — الإصدار 1.2.0

تم توحيد اتجاه التطبيق بالكامل من اليمين إلى اليسار عبر `Directionality` عام، مع تعيين اللغة العربية وتعريب عنوان التطبيق واسم نافذة Windows. أُعيد ترتيب رأس لوحة الخطوات وشريط الإجراءات باستخدام `Wrap` متجاوب حتى تنتقل الأزرار إلى أسطر إضافية بدلاً من التزاحم أو التجاوز.

تم تحسين الثيم العام باستخدام Material 3، وخط `Segoe UI` الملائم لعرض العربية على Windows، وحقول إدخال موحّدة، وبطاقات ذات حواف ومسافات متناسقة. كما تم تحويل الهوامش الثابتة إلى هوامش اتجاهية، وتعريب الوحدات الزمنية وأسماء مفاتيح Windows الظاهرة للمستخدم مع الإبقاء على رموز المفاتيح الداخلية وحقول JSON دون تغيير.

تم الحفاظ على منطق الأتمتة وقنوات Windows، واقتصرت التغييرات على طبقة العرض وتجربة الاستخدام والتسمية. تم رفع الإصدار إلى `1.2.0+5`. يجب تنفيذ `flutter analyze` و`flutter test` و`flutter build windows --release` على جهاز يحتوي Flutter SDK وبيئة Windows للتحقق التنفيذي النهائي.

المراجع التقنية: [Flutter Directionality](https://api.flutter.dev/flutter/widgets/Directionality-class.html)، [Material 3](https://docs.flutter.dev/ui/design/material).


## إصلاح الترجمة المحلية العربية — الإصدار 1.2.1

تمت إضافة اعتماد `flutter_localizations` الرسمي وربطه مع `MaterialApp` عبر `GlobalMaterialLocalizations.delegates`. هذا يعالج خطأ `No MaterialLocalizations found` الذي يظهر عند استخدام عناصر Material مثل `DropdownButton` مع `locale: Locale('ar')`. تم رفع الإصدار إلى `1.2.1+6`.

RTL layout and Arabic text visibility fixes verified in source.

واجهة رئيسية محدثة: رأس التطبيق وشريط القوائم ولوحة الخطوات وشريط التشغيل أصبحت ضمن تخطيط عمودي ممتد بالكامل.

## ميزة تسجيل الماكرو التلقائي — الإصدار 1.3.0

تمت إضافة مسجل ماكرو أصلي لمنصة Windows باستخدام خطافات منخفضة المستوى `WH_KEYBOARD_LL` و`WH_MOUSE_LL`، مع قناة MethodChannel جديدة تتضمن الأوامر `start_macro_recording` و`stop_macro_recording` و`cancel_macro_recording` و`is_macro_recording`.

يحوّل المسجل أحداث ضغطات لوحة المفاتيح والنقرات إلى خطوات سيناريو قابلة للتحرير والتشغيل. تُحفظ المفاتيح مع المعدلات `CTRL` و`ALT` و`SHIFT` و`WIN`، وتُحفظ إحداثيات النقر وزره، كما تُدمج النقرات المتقاربة تلقائياً كنقرة مزدوجة. تُحفظ الفواصل الزمنية بين الأحداث بحد أعلى آمن، وتُهمل أحداث `SendInput` المحقونة حتى لا يعيد تشغيل السيناريو تسجيل نفسه.

على مستوى الأداء، يعمل الالتقاط في خيط Windows مستقل مع قائمة رسائل خاصة، ولا يُرسل حدثاً إلى Flutter أثناء التسجيل؛ بل تُعاد الأحداث دفعة واحدة عند الضغط على إيقاف التسجيل، مما يقلل عبء MethodChannel وإعادة بناء الواجهة. كما يتم إيقاف الخطافات قبل إرجاع النتائج وعند إغلاق الصفحة لضمان عدم بقاء خطاف عالمي فعال.

تتضمن الواجهة أزرار بدء التسجيل وإيقافه وإلغائه، ورسالة حالة واضحة، وتضيف الخطوات الناتجة إلى نهاية السيناريو الحالي مع احترام النافذة المستهدفة المحددة في الواجهة.

> ملاحظة اختبار: يتطلب التسجيل الفعلي بناء وتشغيل نسخة Windows بصلاحيات تسمح بخطافات الإدخال. لا يمكن التحقق من خطافات Win32 داخل بيئة Linux، لذلك يجب تنفيذ `flutter clean` و`flutter pub get` و`flutter analyze` و`flutter test` و`flutter build windows --release` على Windows.


## مراجعة Senior – الإصدار 1.3.1

### الإصلاحات المنفذة

- إضافة `lib/core/app_logger.dart` لتوحيد سجلات debug وinfo والتحذيرات والأخطاء باستخدام `dart:developer` دون مكتبة خارجية.
- تفضيل قناة `keyboard_automation/windows` الأصلية لاستعراض النوافذ، مع إبقاء PowerShell كخطة توافق احتياطية للإصدارات القديمة من Runner.
- فصل تحويل أحداث مسجل الماكرو إلى `AutomationStep.fromMacroEvent` لتقليل منطق الواجهة وجعل التحويل قابلاً للاختبار.
- إضافة حماية عند فشل إيقاف التسجيل أو إغلاق الصفحة أثناء التسجيل، بحيث يطلب التطبيق إلغاء الخطاف الأصلي ولا يتركه فعالاً.
- تحسين الأداء بإعادة استخدام `Random` خارج حلقة التنفيذ وتقليل تأخير إعادة المحاولة اختيارياً عبر الوضع السريع.
- إضافة نافذة **إعدادات الأداء والتشخيص** من قائمة **تعديل** لتعديل عدد المحاولات، الفاصل الدوري، الوضع السريع، وتسجيل التشخيص.
- إضافة اختبارات تحويل أحداث الماكرو للمفاتيح والماوس، وتصفية الأحداث غير المدعومة، مع إبقاء اختبارات JSON القديمة.

### التحقق

نجح `check_keyboard_project.py`. لم يتوفر Flutter SDK أو MSVC داخل بيئة Linux، لذلك لم يمكن تنفيذ `flutter analyze` أو `flutter test` أو `flutter build windows` هنا. يلزم تشغيلها على Windows قبل الإصدار الإنتاجي.


## Modularization Update — Current Development Phase

تم فصل النماذج الأساسية إلى `lib/core/models/automation_step.dart` و`scenario.dart` و`shortcut.dart`. بقيت أسماء حقول JSON وسلوك القراءة القديم كما هما، ولذلك لا تتأثر السيناريوهات المحفوظة سابقاً.

تمت إضافة `lib/core/services/native_automation_service.dart` كحد واحد لقنوات Windows الحالية. حافظت الخدمة على اسمي القناتين `keyboard_automation/keyboard` و`keyboard_automation/windows` وعلى أسماء الاستدعاءات، بينما لم تعد الصفحة الرئيسية مسؤولة مباشرة عن إنشاء القنوات.

أضيفت مهلة بدء مقدارها ثلاث ثوانٍ لمسجل الماكرو الأصلي. إذا لم تكتمل إشارة جاهزية الخيط أو فشل تثبيت خطافات Windows، يتم تغيير حالة التسجيل إلى متوقف قبل انتظار الخيط، لتجنب التعليق أو `join` غير المنتهي.

تم تحديث أداة الفحص المستقلة لتتعرف على مسار خدمة القنوات الجديد، ونجح الفحص بعد الفصل المعماري.


## إصلاح حفظ تسجيل الماكرو تلقائياً

تم تصحيح مسار إيقاف تسجيل الماكرو بحيث لا تظل الخطوات في ذاكرة الواجهة فقط. بعد تحويل الأحداث إلى `AutomationStep`، تُحفظ الخطوات تلقائياً عبر `ScenarioStorage` داخل السيناريو الحالي. إذا لم يكن للسيناريو معرف محفوظ أو كان الاسم الافتراضي `اكتب اسم`، ينشئ التطبيق سيناريوً جديداً باسم زمني من نمط `تسجيل ماكرو YYYY-MM-DD HH-MM`، ثم يحدّث معرف واسم السيناريو في الواجهة.

أصبحت دالة الحفظ تعيد نتيجة منطقية لتمييز نجاح التخزين من فشله، مع تسجيل الخطأ وإظهار تنبيه واضح للمستخدم عند تعذر الحفظ. بقي زر الإلغاء مخصصاً لإلغاء التسجيل دون إضافة أحداث، وحُفظت صيغة JSON الحالية دون تغيير.

تم تشغيل أداة الفحص المستقلة ونجحت. لم يُشغّل `flutter analyze` أو `flutter test` لعدم توفر Flutter SDK في بيئة Linux الحالية.

## الميزات الجديدة: تصدير واستيراد السيناريوهات والمحرر المتقدم

تم فصل تنسيق ملفات السيناريوهات عن الواجهة في `lib/core/services/scenario_file_service.dart`. تستخدم الملفات الجديدة `schemaVersion` و`type` و`id` و`name` و`exportedAt` و`appVersion` و`steps`. يحافظ القارئ على التوافق مع الصيغة القديمة التي كانت عبارة عن قائمة JSON مباشرة، ويتحقق من نوع الملف وإصداره وعدد الخطوات، ويتجاهل العناصر غير الصالحة مع رفض الملف إذا لم يتبق أي خطوة صالحة.

تم تحديث التصدير والاستيراد في `main.dart` ليستخدما الخدمة الجديدة. يتطلب الاستيراد تأكيداً قبل استبدال الخطوات الحالية، ويُعاد توليد معرفات الخطوات لتجنب تصادمها مع السيناريو المفتوح، بينما يحتفظ التصدير بالاسم والمعرف ومعلومات الإصدار. كما أضيفت رسائل خطأ وتسجيلات تشخيصية واضحة.

أضيفت `ScenarioEditorController` في `lib/core/controllers/scenario_editor_controller.dart` لتوفير سجل تراجع وإعادة محدود إلى 50 عملية، مع نسخ عميقة للخطوات لمنع التعديل غير المقصود على التاريخ. دعمت الواجهة البحث في الخطوات، والتراجع، والإعادة، وإعادة الترتيب، والنسخ، والحذف، والمسح الكلي. عند تفعيل البحث يُعطّل السحب وإعادة الترتيب مؤقتاً لأن القائمة المعروضة تكون مصفاة، مع بقاء تعديل الخطوة متاحاً.

أضيفت اختبارات `test/scenario_editor_and_file_test.dart` لاختبار التصدير والاستيراد العربي، الصيغة القديمة، رفض الإصدارات غير المدعومة، تجاهل الخطوات المشوهة، وسلوك التراجع والإعادة.

## GitHub Actions

أضيف الملف `.github/workflows/windows.yml`. ينفذ المسار `flutter pub get` ثم `dart format` و`flutter analyze --fatal-infos` و`flutter test --coverage` على Windows. بعد نجاح الاختبارات يبني `flutter build windows --release`، يضغط مجلد Release في ZIP، ينشئ بصمة SHA-256، ويرفع Artifact لكل عملية. عند دفع tag يبدأ بـ `v` ينشر ZIP والبصمة تلقائياً في GitHub Release باستخدام `GITHUB_TOKEN`.

البيئة المحلية الحالية Linux ولا تحتوي Flutter أو Dart، لذلك يجب اعتماد نجاح Workflow على GitHub أو تشغيل أوامر التحقق على Windows قبل دمج التغييرات.


## إصلاح ظهور خطوات الماكرو فوراً بعد الإيقاف

تم تحسين مسار إيقاف تسجيل الماكرو بحيث تظهر الخطوات الجديدة مباشرة في قائمة خطوات الأتمتة بعد استلام نتيجة التسجيل من طبقة Windows. يشمل الإصلاح مسح مرشح البحث تلقائياً حتى لا يخفي الخطوات المضافة، وتمرير القائمة إلى آخر العناصر المسجلة بعد إعادة بناء الواجهة، مع استمرار الحفظ التلقائي في السيناريو.

زر **إيقاف التسجيل** يحفظ ويعرض الأحداث القابلة للتحويل. أما زر **إلغاء التسجيل** فيبقى مخصصاً لإلغاء التسجيل والتخلص من الأحداث غير المحفوظة، وهو سلوك مقصود لمنع إضافة تسجيل لم يكتمل.


## الإصدار 1.3.4+12 — إصلاحات تقرير Flutter Analyze وRenderFlex

تم إصلاح تخطيط لوحة خطوات الأتمتة باستخدام `LayoutBuilder`. عند ضيق الارتفاع، تتحول اللوحة إلى تخطيط مضغوط قابل للتمرير بدلاً من استخدام `Expanded` داخل مساحة غير كافية، مما يمنع خطأ `RenderFlex overflowed by ... pixels`. في التخطيط الطبيعي تبقى القائمة موسعة وتدعم إعادة الترتيب والبحث.

تم أيضاً تحديث استخدامات `PopScope` إلى `onPopInvokedWithResult`، واستبدال `RawKeyboardListener` و`RawKeyEvent` بواجهتي `KeyboardListener` و`KeyEvent`، واستبدال `value` بـ`initialValue` في `DropdownButtonFormField`، وإزالة الاستيراد والحقول غير المستخدمة، ومعالجة إلغاء التسجيل دون `return` داخل `finally`.

بعد إيقاف تسجيل الماكرو، تُضاف الخطوات إلى القائمة فوراً ويُمسح مرشح البحث وتنتقل القائمة إلى آخر الخطوات. أما زر إلغاء التسجيل فيتخلص من الأحداث ولا يضيفها إلى السيناريو، وهو السلوك المقصود.

تعذر تشغيل Flutter Analyze/Test وWindows Build في بيئة Linux الحالية لعدم توفر Flutter SDK وMSVC. نجح الفحص البنيوي المستقل للمشروع.

## الإصدار 1.3.5+13 — إصلاح عدم ظهور خطوات الماكرو بعد الإيقاف

تم إصلاح مسار إيقاف تسجيل الماكرو من الطرف الأصلي إلى Flutter. يعيد Windows الآن نتيجة ذاتية الوصف تحتوي على `events` و`count`، مع إبقاء Dart متوافقاً مع النسخ القديمة التي كانت تعيد قائمة مباشرة. يقوم Flutter بتحويل كل حدث إلى `AutomationStep`، ويقبل صيغ الحقول البديلة مثل `kind` و`action` و`keyName` و`mouseButton`، ثم يضيف الخطوات إلى `_steps` فوراً داخل `setState` ويمسح مرشح البحث ويحفظ السيناريو تلقائياً.

أضيفت سجلات تشخيصية لعدد الأحداث الأصلية، وعدد الخطوات المحولة، وعدد الأحداث المرفوضة. كما أصبحت الرسالة الظاهرة للمستخدم تفرق بين عدم إعادة Windows لأي أحداث وبين إعادة أحداث غير قابلة للتحويل. أضيف اختبار وحدات للتحقق من تحويل صيغ payload البديلة إلى خطوات لوحة مفاتيح وفأرة.


## الإصدار 1.3.6+14 — إصلاح حوار الماوس وتسجيل النافذة النشطة

تم إصلاح `RenderFlex overflow` داخل حوار تعديل نقرة الماوس. أصبح محتوى الحوار داخل مساحة ذات ارتفاع متوافق مع حجم النافذة ويستخدم `SingleChildScrollView`، ولذلك يمكن الوصول إلى حقلي التكرار والتأخير في الشاشات القصيرة دون ظهور الشريط الأصفر.

تم تطوير مسجل Windows ليقرأ عنوان النافذة الأمامية عبر `GetForegroundWindow` و`GetWindowTextW` عند التقاط كل حدث لوحة مفاتيح أو فأرة. يرفق المسجل العنوان في الحقول `window` و`targetWindow` داخل payload، وتحافظ طبقة Dart على هذا العنوان داخل `AutomationStep.targetWindow` مع دعم fallback للصيغ القديمة. يظهر اسم النافذة ضمن وصف الخطوة ويُستخدم عند تشغيلها لتفعيل النافذة المستهدفة قبل تنفيذ الأمر.

أضيف اختبار وحدات يثبت أن عنوان النافذة القادم من الحدث يُحفظ في الخطوة الناتجة، ورفع رقم الإصدار إلى `1.3.6+14`. يجب اختبار بناء Windows فعلياً على جهاز Windows لأن بيئة Linux لا توفر Flutter SDK أو MSVC.

كل خطوة لوحة مفاتيح أو فأرة تسجل اسم النافذة التي كانت في الواجهة الأمامية لحظة الحدث. إذا تغيرت النافذة أثناء التسجيل، يمكن أن تحمل الخطوات اللاحقة عنواناً مختلفاً، وهو السلوك المقصود لتشغيل سيناريو متعدد النوافذ.

## الإصدار 1.3.7+15 — كشف انتقال النافذة أثناء تسجيل الماكرو

تم تطوير `MacroRecorder` لاكتشاف تغير عنوان النافذة بين حدثين متتاليين. عند تسجيل أول حدث في نافذة جديدة، يرفق payload الحقول `windowChanged: true` و`previousWindow` إلى جانب `window` و`targetWindow`. أضيف احتياط لأحداث الفأرة يعتمد على عنوان النافذة الموجودة عند إحداثيات الحدث باستخدام `WindowFromPoint` و`GetAncestor(GA_ROOT)`، مع تصفير حالة النافذة السابقة عند بدء تسجيل جديد.

تم تحديث `AutomationStep` لحفظ `windowChanged` و`previousWindow`، وقراءتهما من payload الأصلي ومن ملفات JSON، وتصديرهما مع السيناريو مع الحفاظ على توافق ملفات السيناريو القديمة. تعرض بطاقة الخطوة في الواجهة العربية RTL شارة واضحة بصيغة «انتقال من» و«إلى»، مع قص النص إلى سطر واحد لمنع عودة `RenderFlex overflow`. كما أصبح البحث يشمل اسمي النافذة السابقة والجديدة.

أضيف اختبار وحدات يثبت تحويل حدث الانتقال وحفظ الحقلين عبر دورة `toJson` و`fromJson`. رُفع رقم الإصدار إلى `1.3.7+15`. لم يُنفذ بناء Windows الفعلي في بيئة Linux لعدم توفر Flutter SDK وMSVC، ولذلك يجب إكمال اختبار `flutter analyze` و`flutter test` و`flutter build windows --release` على جهاز Windows.

---

## ملاحظات التحقق

نجح الفحص البنيوي المستقل للمشروع بعد التعديلات. أما التحقق من خطافات Win32 وسلوك عنوان النافذة الفعلي فيتطلب تشغيل النسخة على Windows، لأن بيئة Linux لا توفر واجهات Win32 ولا مترجم MSVC.

## مراجعة شاملة إضافية — الإصدار 1.3.7+15

تمت مراجعة دورة حياة صفحة Flutter ومسارات التسجيل والإيقاف والاستيراد والتصدير والتنفيذ، إضافة إلى حدود MethodChannel وطبقة Win32 الخاصة بالنوافذ والإدخال. تم تصفير حالات العدادات عند استيراد سيناريو جديد حتى لا تنتقل قيم تشغيلية من السيناريو السابق، وربط `appVersion` في ملفات JSON بثابت مركزي بدلاً من رقم إصدار قديم مضمّن داخل `main.dart`، وتحسين إلغاء تسجيل الماكرو عند إغلاق الصفحة أو إلغائه من الواجهة.

في طبقة Windows، تم تحصين قراءة الأعداد في محرك التنفيذ برفض `NaN` و`Infinity` قبل التحويل، وتطبيع اسم زر الفأرة من حيث المسافات وحالة الأحرف، وتطبيق منطق انتقال النافذة على أحداث الفأرة التي كانت تُضاف مباشرة إلى قائمة الأحداث، والتحقق من نوع `windowTitle` قبل استخدامه في قناة النوافذ. كما تم توحيد النافذة الجذرية في تعداد النوافذ حتى تتطابق العناوين مع ما يلتقطه المسجل.

تم تحديث اختبارات خدمة الملفات لاستخدام الإصدار المركزي والتحقق من حفظه في المستند المصدر. نجح `check_keyboard_project.py` بعد التعديلات. لا يتوفر Flutter أو Dart أو MSVC في بيئة المراجعة الحالية، ولذلك لم يمكن تنفيذ التحليل الساكن أو اختبارات Dart أو بناء Windows فعلياً.

يجب على بيئة Windows تنفيذ الأوامر التالية قبل اعتماد الإصدار للتوزيع:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter build windows --release
```

بعد ذلك يجب اختبار سيناريو متعدد النوافذ يفتح نافذة التشغيل، يكتب اسم برنامج، يضغط Enter، وينفذ خطوة لاحقة داخل البرنامج الجديد، مع التأكد من ظهور `targetWindow` ووسم الانتقال وعدم تنفيذ الخطوة في نافذة خاطئة.
